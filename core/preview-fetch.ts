
export default async function PreviewFetch() {
    try {
        // input으로 movieTitle를 받음.
        // firebase에서 {image} 정보를 가져옴.
        // image없을 시 kmdb에서 fetch title를 통해 
        // image 링크 가져와서 firebase에 등록
        // image 링크를 반환.
    } catch {

    }
}