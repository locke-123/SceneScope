
export default async function DetailFetch() {
    try {
        // input으로 movieCode을 받음.
        // 기본적으로는 detail 정보를 반환함.
        // 이는 kobis의 searchMovieInfo 를 사용.
        // detail 정보 반환하되 가급적 firebase 것을 사용함.
        // 이는 내부에 들어가서 영화 상세 정보를 조회하기 위함.
    } catch {

    }
}